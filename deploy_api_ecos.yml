---
 - hosts: all
   become: true
   become_user: root
   become_method: sudo
   tasks:
           - name: Transfer the script
             copy: src=/var/lib/awx/projects/myrepo/deploy.sh dest=/home/ansible owner=ansible group=ansible mode=0777 remote_src=yes
           - name: deploy jboss
             script: sh /home/ansible/deploy.sh 
                  

