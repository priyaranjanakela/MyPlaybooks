---
 - hosts: all
   become: true
   become_user: root
   become_method: sudo
   tasks:
           - name: deploy jboss
             script: /var/lib/awx/projects/myrepo/deploy.sh 
                  

