---
 - hosts: all
   become: true
   become_user: root
   become_method: sudo
   tasks:
           - name: Transfer the script
             copy: src=/opt/deploy.sh dest=/home/ansible mode=0777
           - name: deploy jboss
             script: /home/ansible/deploy.sh 
                  

