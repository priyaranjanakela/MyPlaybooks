---
 - hosts: all
   become: true
   vars:
     - script: "./deploy.sh"
   tasks:
   - name: Transfer the script
     copy: src=/opt/deploy.sh dest=/home/ansible owner=ansible group=ansible mode=0777 
   - name: deploy jboss
     shell:
        "{{ script | quote}} {{ item.instance }}" 
     with_items:
       - "{{ instance: url1 }}"
     args:
        chdir: "/home/ansible/"
        executable: /bin/bash 
                  

